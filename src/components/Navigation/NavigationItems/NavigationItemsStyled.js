import styled from 'styled-components';

const Wrapper = styled.ul`
  margin-left: auto;
  padding: 0;
  list-style: none;
  display: flex;
  flex-flow: row;
  height: 100%;
`;

export default Wrapper;
